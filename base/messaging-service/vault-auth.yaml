---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: messaging-service-vault-auth
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  vaultConnectionRef: default
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: messaging-service
    serviceAccount: messaging-service-sa
    audiences:
      - vault
