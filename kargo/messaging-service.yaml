---
apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: messaging-service-images
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  subscriptions:
    - image:
        repoURL: ghcr.io/whispr-messenger/messaging-service/messaging-service
        imageSelectionStrategy: NewestBuild
        allowTags: "^sha-.+$"
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: messaging-service-staging
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: messaging-service-images
      sources:
        direct: true
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/whispr-messenger/deploy.git
            checkout:
              - branch: main
                path: ./deploy
        - uses: kustomize-set-image
          config:
            path: ./deploy/overlays/staging/messaging-service
            images:
              - image: ghcr.io/whispr-messenger/messaging-service/messaging-service
                tag: ${{ imageFrom("ghcr.io/whispr-messenger/messaging-service/messaging-service").Tag }}
        - uses: git-commit
          config:
            path: ./deploy
            messageTemplate: |
              chore(staging): promote messaging-service
        - uses: git-push
          config:
            path: ./deploy
        - uses: argocd-update
          config:
            apps:
              - name: messaging-service-staging
                sources:
                  - repoURL: https://github.com/whispr-messenger/deploy.git
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: messaging-service-production
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: messaging-service-images
      sources:
        stages:
          - messaging-service-staging
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/whispr-messenger/deploy.git
            checkout:
              - branch: main
                path: ./deploy
        - uses: kustomize-set-image
          config:
            path: ./deploy/overlays/production/messaging-service
            images:
              - image: ghcr.io/whispr-messenger/messaging-service/messaging-service
                tag: ${{ imageFrom("ghcr.io/whispr-messenger/messaging-service/messaging-service").Tag }}
        - uses: git-commit
          config:
            path: ./deploy
            messageTemplate: |
              chore(production): promote messaging-service
        - uses: git-push
          config:
            path: ./deploy
        - uses: argocd-update
          config:
            apps:
              - name: messaging-service-production
                sources:
                  - repoURL: https://github.com/whispr-messenger/deploy.git
