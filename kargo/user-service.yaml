---
apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: user-service-images
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  subscriptions:
    - image:
        repoURL: ghcr.io/whispr-messenger/user-service/user-service
        imageSelectionStrategy: NewestBuild
        allowTags: "^sha-[a-f0-9]{7}$"
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: user-service-staging
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: user-service-images
      sources:
        direct: true
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/whispr-messenger/deploy.git
            checkout:
              - branch: main
                path: ./deploy
        - uses: kustomize-set-image
          config:
            path: ./deploy/overlays/staging/user-service
            images:
              - image: ghcr.io/whispr-messenger/user-service/user-service
                tag: ${{ imageFrom("ghcr.io/whispr-messenger/user-service/user-service").Tag }}
        - uses: git-commit
          config:
            path: ./deploy
            message: |
              chore(staging): promote user-service
        - uses: git-push
          config:
            path: ./deploy
        - uses: argocd-update
          config:
            apps:
              - name: user-service-staging
                sources:
                  - repoURL: https://github.com/whispr-messenger/deploy.git
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: user-service-production
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: user-service-images
      sources:
        stages:
          - user-service-staging
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/whispr-messenger/deploy.git
            checkout:
              - branch: main
                path: ./deploy
        - uses: kustomize-set-image
          config:
            path: ./deploy/overlays/production/user-service
            images:
              - image: ghcr.io/whispr-messenger/user-service/user-service
                tag: ${{ imageFrom("ghcr.io/whispr-messenger/user-service/user-service").Tag }}
        - uses: git-commit
          config:
            path: ./deploy
            message: |
              chore(production): promote user-service
        - uses: git-push
          config:
            path: ./deploy
        - uses: argocd-update
          config:
            apps:
              - name: user-service-production
                sources:
                  - repoURL: https://github.com/whispr-messenger/deploy.git
