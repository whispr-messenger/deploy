---
apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: auth-service-images
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  subscriptions:
    - image:
        repoURL: ghcr.io/whispr-messenger/auth-service/auth-service
        imageSelectionStrategy: NewestBuild
        allowTags: "^sha-.+$"
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: auth-service-staging
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: auth-service-images
      sources:
        direct: true
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/whispr-messenger/deploy.git
            checkout:
              - branch: main
                path: ./deploy
        - uses: kustomize-set-image
          config:
            path: ./deploy/overlays/staging/auth-service
            images:
              - image: ghcr.io/whispr-messenger/auth-service/auth-service
        - uses: git-commit
          config:
            path: ./deploy
            messageTemplate: |
              chore(staging): promote auth-service
        - uses: git-push
          config:
            path: ./deploy
        - uses: argocd-update
          config:
            apps:
              - name: auth-service-staging
                sources:
                  - repoURL: https://github.com/whispr-messenger/deploy.git
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: auth-service-production
  namespace: whispr
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: auth-service-images
      sources:
        stages:
          - auth-service-staging
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/whispr-messenger/deploy.git
            checkout:
              - branch: main
                path: ./deploy
        - uses: kustomize-set-image
          config:
            path: ./deploy/overlays/production/auth-service
            images:
              - image: ghcr.io/whispr-messenger/auth-service/auth-service
        - uses: git-commit
          config:
            path: ./deploy
            messageTemplate: |
              chore(production): promote auth-service
        - uses: git-push
          config:
            path: ./deploy
        - uses: argocd-update
          config:
            apps:
              - name: auth-service-production
                sources:
                  - repoURL: https://github.com/whispr-messenger/deploy.git
