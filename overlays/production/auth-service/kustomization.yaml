apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: whispr-prod

resources:
  - ../../../base/auth-service
  - configmap.yaml

labels:
  - pairs:
      environment: production
    includeSelectors: false

images:
  - name: ghcr.io/whispr-messenger/auth-service/auth-service
    newTag: sha-38acfc0
