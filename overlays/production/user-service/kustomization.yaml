apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: whispr-prod

resources:
  - ../../../base/user-service
  - configmap.yaml

labels:
  - pairs:
      environment: production
    includeSelectors: false

images:
  - name: ghcr.io/whispr-messenger/user-service/user-service
    newTag: sha-57bb5a8
