apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: whispr-prod

resources:
  - ../../../base/messaging-service
  - configmap.yaml

labels:
  - pairs:
      environment: production
    includeSelectors: false

images:
  - name: ghcr.io/whispr-messenger/messaging-service/messaging-service
    newTag: sha-6f2362a
