apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: whispr-staging
resources:
- ../../../base/media-service
- configmap.yaml
labels:
- pairs:
    environment: staging
  includeSelectors: false

patches:
- target:
    group: secrets.hashicorp.com
    version: v1beta1
    kind: VaultAuth
    name: media-service-vault-auth
  patch: |
    - op: replace
      path: /spec/kubernetes/role
      value: media-service-staging
- target:
    group: secrets.hashicorp.com
    version: v1beta1
    kind: VaultDynamicSecret
    name: media-service-db-creds
  patch: |
    - op: replace
      path: /spec/path
      value: creds/role_media_service_staging
- target:
    group: secrets.hashicorp.com
    version: v1beta1
    kind: VaultDynamicSecret
    name: media-service-redis-creds
  patch: |
    - op: replace
      path: /spec/path
      value: creds/redis_role_media_service_staging

images:
- name: ghcr.io/whispr-messenger/media-service/media-service
  newTag: sha-398b7b6
